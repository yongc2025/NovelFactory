# ACCEPTANCE

- [x] 新生成的 `world.json` 包含前端需要的世界观字段。
- [x] 旧的 `[{category, content}]` 数据仍能被 API 转换显示。
- [x] `key_locations/rules/constraints` 不再因契约转换默认全空。
- [x] 后续角色/大纲阶段兼容 dict/list 两种世界观结构。
- [x] 前端构建通过。