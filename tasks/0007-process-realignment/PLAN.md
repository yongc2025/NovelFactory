# PLAN — 流程回正

1. 复核根目录与父级 `AGENTS.md`，确认当前约束。
2. 读取 `tasks/INDEX.md` 与现有任务包，确认任务系统现状。
3. 审计工作树、任务状态、前后端契约、文档同步偏离点。
4. 创建 `0007-process-realignment` 完整任务包。
5. 新增 `docs/process-realignment-audit.md`，记录偏离、影响、回正路径。
6. 同步 `tasks/INDEX.md` 与 `docs/TASKS.md`。
7. 输出后续必须按任务包执行的修复顺序。
