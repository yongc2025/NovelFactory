# ACCEPTANCE — 前端契约修复

- [x] 概览页不再显示阶段确认卡。
- [x] 只有当前 tab 对应后端阶段处于等待确认时，才显示阶段操作卡。
- [x] 选题页通过单个方案卡片完成采用；顶部操作卡只提供反馈与重新生成。
- [x] 世界观、角色、大纲等页面顶部可直接反馈或重新生成，无需滚动到底部。
- [x] `characters` tab 调用后端 `character` 阶段，`chapters` tab 调用后端 `draft` 阶段。
- [x] 重新生成期间有遮罩/进度提示，且按钮不能重复触发。
- [x] `npm run build` 通过。
